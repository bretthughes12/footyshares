<%= semantic_form_for @user, :as => :signup, :url => {:action => :create} do |f| %>
  <%= f.inputs do %>
    <%= f.input :login, label: 'Username' %>
    <%= f.input :email %>
    <%= f.input :name, label: 'Your real name', hint: 'First name and surname, please' %>
    <%= f.input :nickname, hint: 'Leave blank to use your real name on the ladder' %>
    <%= f.input :password %>
    <%= f.input :password_confirmation, label: 'Repeat Password' %>
    <%= f.input :starting_shares, label: 'Shares', hint: 'Starting share price is 10 cents per share, sold in lots of 50, minimum purchase is 50 shares', input_html: {size: 4} %>
  <% end %>

  <%= f.actions do %>
    <%= f.action :submit, label: 'Sign Up'  %>
  <% end %>
<% end %>
