<%= semantic_form_for @share do |f| %>
  <%= f.inputs do %>
    <%= f.input :team, collection: Round.current.teams %>
    <%= f.input :user, collection: User.all %>
    <%= f.input :shares %>
  <% end %>

  <%= f.actions do %>
    <%= f.action :submit, :as => :input %>
  <% end %>
<% end %>
